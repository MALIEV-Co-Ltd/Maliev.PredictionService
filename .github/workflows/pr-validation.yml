name: PR Validation
on:
  pull_request:
    branches: [main, develop, staging]
permissions:
  contents: read
concurrency:
  group: prediction-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  validate:
    uses: ./.github/workflows/_validate.yml
